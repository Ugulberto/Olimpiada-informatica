#include <bits/stdc++.h>
using namespace std;

bool solve() {
    int rotaciones;
    cin >> rotaciones;
    string nombre; cin >> nombre;
    string linea;
    getline(cin, linea);
    
    for (int i = 0; i < linea.size(); i++)
    {
        int c = linea[i];
        
    }
    
    return true;
}

int main() {
    while (solve());
    return 0;
}